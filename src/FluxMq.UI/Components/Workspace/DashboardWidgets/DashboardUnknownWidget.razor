@namespace FluxMq.UI.Components.Workspace

<div class="dashboard-widget-empty" role="status" aria-live="polite">@Type</div>

@code {
    [Parameter, EditorRequired]
    public string Type { get; set; } = string.Empty;
}
